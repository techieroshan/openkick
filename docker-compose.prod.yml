services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: openkick
      POSTGRES_PASSWORD: openkick_password
      POSTGRES_DB: openkick
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openkick"]
      interval: 10s
      timeout: 5s
      retries: 5

  bff:
    build:
      context: .
      dockerfile: docker/bff.Dockerfile
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://openkick:openkick_password@db:5432/openkick?schema=public
    expose:
      - "3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: docker/web.Dockerfile
    depends_on:
      bff:
        condition: service_healthy
    ports:
      - "11701:80"
    restart: unless-stopped

volumes:
  postgres_data:
